type Mutation {
    # Virgula Opcional
    novoUsuario(
        dados: UsuarioInput!
    ): Usuario!

    excluirUsuario(
        filtro: UsuarioFiltro!
    ): Usuario

    alterarUsuario(
        id: Int!
        nome: String,
        email: String,
        idade: Int
    ): Usuario!

}