type Mutation {
    # Virgula Opcional
    novoUsuario(
        dados: UsuarioInput!
    ): Usuario!

    excluirUsuario(id: Int!): Usuario

    alterarUsuario(
        id: Int!
        nome: String,
        email: String,
        idade: Int
    ): Usuario!

}