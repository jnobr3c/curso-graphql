type Mutation {
    # Virgula Opcional
    novoUsuario(nome: String,
        email: String,
        idade: Int
    ): Usuario!

    excluirUsuario(id: Int!): Usuario

    alterarUsuario(
        id: Int!
        nome: String,
        email: String,
        idade: Int
    ): Usuario!

}